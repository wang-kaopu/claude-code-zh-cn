# Native latest candidate failure: 2.1.153

- Run: https://github.com/wang-kaopu/claude-code-zh-cn/actions/runs/26560043082
- Head SHA: `5889dc056e205722d637303b61e6140e50b9be51`
- Status: `fail`
- Candidate kind: `native`
- Native: `darwin-arm64` / `ok` / `ok` / `ok`
- Display audit: `fail` (9 issues / 11 commands)

## What Failed

- Candidate verification did not pass: `status=fail`.
- Display audit did not pass: `status=fail`.
- Summary: pass=0, fail=1, skip=0.

## Display Audit Issues

- `top_json_schema_term` (`top_help`, display-preserve)

  ```text
  JSON Schema
  ```

- `top_help_line_13` (`top_help`, display-untranslated-line)

  ```text
  code", "prompt": "You are a code
  ```

- `top_help_line_45` (`top_help`, display-untranslated-line)

  ```text
  high, xhigh, max）
  ```

- `top_help_line_51` (`top_help`, display-untranslated-line)

  ```text
  --fallback-model <model>              Enable automatic fallback to specified
  ```

- `top_help_line_52` (`top_help`, display-untranslated-line)

  ```text
  model when the default model is
  ```

- `top_help_line_53` (`top_help`, display-untranslated-line)

  ```text
  overloaded or not available (only works
  ```

- `top_help_line_54` (`top_help`, display-untranslated-line)

  ```text
  with --print)
  ```

- `top_help_line_56` (`top_help`, display-untranslated-line)

  ```text
  --file file_abc:doc.txt
  ```

- `top_help_line_57` (`top_help`, display-untranslated-line)

  ```text
  file_def:img.png）
  ```

## Text Diff Excerpt

```markdown
# Upstream text diff: 2.1.150 -> 2.1.153

- Added upstream strings: 6045
- Removed upstream strings: 4587
- Already covered by translations: 35
- Needs translation review: 6010
- Sensitive review hints: 135

## Added strings needing review
- - **${...}** (if available) - Run a multi-step subagent pipeline; prefer it over hand-orchestrating ${...} calls when a matching workflow exists
- - **id**: Task identifier (use with TaskGet, TaskUpdate)
- - `run_in_background` runs the command detached: it keeps running across turns and re-invokes you when it exits. No `&` needed.
- - ${...} (task ${...})
- - Before assigning tasks to teammates, to see what's available
- - End git commit messages with: ${...}
- - End PR bodies with: ${...}
- - Include enough detail in the description for another agent to understand and complete the task - New tasks are created with status 'pending' and no owner - use TaskUpdate with the `owner` parameter to assign them
- - Working directory persists between calls, but prefer absolute paths \u2014 `cd` in a compound command can trigger a permission prompt. Shell state (env vars, functions) does not persist; the shell is initialized from the user's profile.
- - You can use the `run_in_background` parameter to run the command in the background. Only use this if you don't need the result immediately and are OK being notified when the command completes later. You do not need to check the output right away - you'll be notified when it finishes.
- --add-dir
- --auto-correct
- --cwd <dir>
- --cwd is required
- --fix
- --in-place=
- --lines
- --lines=
- --number
- --reply-on-resume
- --write
- -//Apple//DTD PLIST 1.0//EN
- -jpeg
- , ${...} ${...} recalled
- , ${...} conflict(s)
- , failures pull=${...} push=${...}
- , matches: ${q.join(", ")} `),process.exit(1);let K=q[0],O=await o7(b4(K)),{removed:T,error:z,keptWorktree:$,keptReason:Y}=await dqH(K);if(!T)uH("cli_bg_rm","kill_unconfirmed"),process.stderr.write(`couldn
- , or an integer."),shell:KW().optional().describe("Shell for
- , reaped ${...} tombstoned
- , soft-deleted ${...}
- ,_,q),debug:yI_(
- ,_,q),info:yI_(
- ,_,q),warn:yI_(
- ,_.abortController.signal,_.options.isNonInteractiveSession):null,s?u46(H.command,S_(),r,
- ,_.abortController.signal,_.options.isNonInteractiveSession):null]);if(_.abortController.signal.aborted)throw new zT;if(_H)t2K(H.command,
- ,_.id)}`;$6(H,`Using claude.ai proxy at ${x}`);let U=Iu3(NH_(globalThis.fetch)),g=Vw({url:x}),Q={fetch:Ic8(U,_),requestInit:{...g,headers:{
- ,_.nested.google.nested.protobuf.nested),yoH.common(
- ,_.querySource);let w=aJ();if(w?.agentId)A.setAttribute(
- ,_),K=await fetch(q,{...Vw({url:String(q)}),headers:{Accept:
- ,_),K=I5.join(H,
- ... 5970 more

## Added strings already covered
- [Image data detected and sent to Claude]
- [View Tab]
- Access denied to specific paths outside allowed directories
- Accessibility and Screen Recording permissions are required. Call request_access to show the permission panel.
- Another Claude session is currently using the computer. Wait for that session to finish, or find a non-computer-use approach.
- Claude explains its implementation choices and codebase patterns
- Claude is waiting for your input
- Claude pauses and asks you to write small pieces of code for hands-on practice
- Commands cannot run outside the sandbox under any circumstances.
- Compact buffer
- Computer control is disabled in Settings. Enable it and try again.
- Conversation too long. Press esc twice to go up a few messages and try again.
- Enter to continue
- Evidence of sandbox-caused failures includes:
- Failed to generate conversation summary - response did not contain valid text content
- Key pressed.
- Network connection failures to non-whitelisted hosts
- No scheduled jobs
- Not enough messages to compact.
- Nothing to summarize after the selected message.
- Nothing to summarize before the selected message.
- Plan needs revision
- Plan updated
- Released computer-use lock
- Removed worktree
- Returned to
- sandbox disabled
- Tab created
- The sandbox has the following restrictions:
- This command requires approval
- to toggle
- toggle transcript
- Tool use is not allowed during compaction
- When issuing multiple commands:
- When you see evidence of sandbox-caused failure:

## Removed strings
- ----FormBoundary${...}
- --agent-color ${...}
- --agent-id ${...}
- --agent-name ${...}
- --agent-type ${...}
- --model ${...}
- --parent-session-id ${...}
- --permission-mode acceptEdits
- --permission-mode auto
- --plugin-dir ${...}
- --plugin-url ${...}
- --settings ${...}
- --team-name ${...}
- , got
- , matches: ${q.join(", ")} `),process.exit(1);let K=q[0],O=await c7(b4(K)),{removed:T,error:z,keptWorktree:$,keptReason:A}=await a8H(K);if(!T)uH("cli_bg_rm","kill_unconfirmed"),process.stderr.write(`couldn
- , or an integer."),shell:S2().optional().describe("Shell for
- ,_,q),debug:UE_(
- ,_,q),info:UE_(
- ,_,q),warn:UE_(
- ,_.nested.google.nested.protobuf.nested),fiH.common(
- ,_.querySource);let w=UJ();if(w?.agentId)Y.setAttribute(
- ,_),K=await fetch(q,{...Yj({url:String(q)}),headers:{Accept:
- ,_),priority:
- ,_)(`${K.connected} connected`));if(K.needsAuth)O.push(Eq(
- ,_)(`${K.disabled} disabled`));if(K.failed)O.push(Eq(
- ,_)(`${K.needsAuth} need auth`));if(K.pending)O.push(Eq(
- ,_)(`${K.pending} pending`));if(K.disabled)O.push(Eq(
- ,_)}}nqT=Symbol(
- ,_)}else if(eBH===
- ,_[3]=w,_[4]=j;else j=_[4];let J=j,D;if(_[5]===Symbol.for(
- ,_],{cwd:q});if(Y!==0)N(`Could not delete agent worktree branch: ${w}`,{level:
- ,_]);if(lr6.env.AWS_EXECUTION_ENV)K.push([
- ,_H),e.destroy()})}),W.once(
- ,_h9=
- ,_N9=
- ,_v9=
- ,_V9=
- ,_y9=
- ,!0),A(
- ,!0);if(w)_.push(w)}else{let w=Kq6(H,
- ... 4547 more
```

## Takeover Commands

```bash
node scripts/verify-upstream-compat.js --baseline 2.1.153 --skip-latest --native-macos-arm64 --json
node scripts/generate-upstream-text-diff.js --to 2.1.153 --native-macos-arm64
node scripts/promote-native-candidate.js --candidate <candidate-json>
```

## Done Criteria

- Add the missing source-of-truth translations or guard fix on this branch.
- Re-run the native latest candidate workflow for this version.
- Only promote support metadata after native verification and display audit pass.
- Remove or update this handoff report before marking the PR ready.
