# Native latest candidate failure: 2.1.227

- Run: https://github.com/wang-kaopu/claude-code-zh-cn/actions/runs/31459280212
- Head SHA: `1941cfc02e5975a0f9ab86126ca63905fe4fe6c1`
- Status: `fail`
- Candidate kind: `native`
- Native: `darwin-arm64` / `ok` / `ok` / `ok`
- Display audit: `fail` (88 issues / 11 commands)

## What Failed

- Candidate verification did not pass: `status=fail`.
- Display audit did not pass: `status=fail`.
- Summary: pass=0, fail=1, skip=0.

## Display Audit Issues

- `top_help_line_19` (`top_help`, display-untranslated-line)

  ```text
  --autocompact <auto|tokens>           èªå¨åç¼© window size (auto, or 100k–1M
  ```

- `top_help_line_21` (`top_help`, display-untranslated-line)

  ```text
  --ax-screen-reader                    Render screen-reader friendly output
  ```

- `top_help_line_22` (`top_help`, display-untranslated-line)

  ```text
  (flat text, no decorative borders or
  ```

- `top_help_line_24` (`top_help`, display-untranslated-line)

  ```text
  --bg, --background                    Start the session as a background agent
  ```

- `top_help_line_25` (`top_help`, display-untranslated-line)

  ```text
  and return immediately (manage with
  ```

- `top_help_line_35` (`top_help`, display-untranslated-line)

  ```text
  ä»å¯éè¿ /skill-name è§£æãè¯·æ¾å¼æä¾ä¸ä¸æï¼--system-prompt[-file]ã--append-system-prompt[-file]ã--add-dirï¼CLAUDE.md
  ```

- `top_help_line_37` (`top_help`, display-untranslated-line)

  ```text
  --betas <betas...>                    è¦åå«å¨ API è¯·æ±ä¸­ç Beta headerï¼ä» API
  ```

- `top_help_line_41` (`top_help`, display-untranslated-line)

  ```text
  --cloud [description|session_id|url]  Create a cloud session with the given
  ```

- `top_help_line_42` (`top_help`, display-untranslated-line)

  ```text
  description, or attach to an existing
  ```

- `top_help_line_43` (`top_help`, display-untranslated-line)

  ```text
  one by session ID or claude.ai/code URL
  ```

- `top_help_line_54` (`top_help`, display-untranslated-line)

  ```text
  --environment <environment_id>        Create a new cloud session that runs on
  ```

- `top_help_line_55` (`top_help`, display-untranslated-line)

  ```text
  the given self-hosted environment
  ```

- `top_help_line_58` (`top_help`, display-untranslated-line)

  ```text
  å°æ¯å°æºå¨ä¸åçä¿¡æ¯æ®µï¼cwdãenv ä¿¡æ¯ãmemory è·¯å¾ãgit ç¶æï¼ä»ç³»ç»æç¤ºè¯ç§»å°ç¬¬ä¸æ¡ç¨æ·æ¶æ¯ä¸­ãè¿æ ·å¯æé«è·¨ç¨æ· prompt cache
  ```

- `top_help_line_59` (`top_help`, display-untranslated-line)

  ```text
  å¤ç¨ãä»éç¨äºé»è®¤ç³»ç»æç¤ºè¯ï¼ä½¿ç¨ --system-prompt æ¶å¿½ç¥ï¼ã (é»è®¤ï¼ false)
  ```

- `top_help_line_60` (`top_help`, display-untranslated-line)

  ```text
  --fallback-model <model>              Enable automatic fallback to specified
  ```

- `top_help_line_61` (`top_help`, display-untranslated-line)

  ```text
  model(s) when the default model is
  ```

- `top_help_line_62` (`top_help`, display-untranslated-line)

  ```text
  overloaded or not available. Accepts a
  ```

- `top_help_line_63` (`top_help`, display-untranslated-line)

  ```text
  comma-separated list to try each in
  ```

- `top_help_line_64` (`top_help`, display-untranslated-line)

  ```text
  order. Re-tries the primary at the start
  ```

- `top_help_line_65` (`top_help`, display-untranslated-line)

  ```text
  of each user turn. (only works with
  ```

- `top_help_line_67` (`top_help`, display-untranslated-line)

  ```text
  --file <specs...>                     å¯å¨æ¶ä¸è½½æä»¶èµæºãæ ¼å¼ï¼file_id:relative_pathï¼ä¾å¦
  ```

- `top_help_line_69` (`top_help`, display-untranslated-line)

  ```text
  file_def:img.pngï¼
  ```

- `top_help_line_70` (`top_help`, display-untranslated-line)

  ```text
  --fork-session                        æ¢å¤ä¼è¯æ¶åå»ºæ°ç session IDï¼èä¸æ¯å¤ç¨å session IDï¼ä¸
  ```

- `top_help_line_72` (`top_help`, display-untranslated-line)

  ```text
  --forward-subagent-text               Forward subagent text and thinking
  ```

- `top_help_line_73` (`top_help`, display-untranslated-line)

  ```text
  blocks as assistant/user messages with
  ```

- `top_help_line_74` (`top_help`, display-untranslated-line)

  ```text
  parent_tool_use_id set (only works with
  ```

- `top_help_line_75` (`top_help`, display-untranslated-line)

  ```text
  --print and --output-format=stream-json)
  ```

- `top_help_line_91` (`top_help`, display-untranslated-line)

  ```text
  --model <model>                       Model for the current session. Provide
  ```

- `top_help_line_92` (`top_help`, display-untranslated-line)

  ```text
  an alias for the latest model (e.g.
  ```

- `top_help_line_94` (`top_help`, display-untranslated-line)

  ```text
  model's full name (e.g.
  ```

- `top_help_line_126` (`top_help`, display-untranslated-line)

  ```text
  --safe-mode                           Start with all customizations
  ```

- `top_help_line_127` (`top_help`, display-untranslated-line)

  ```text
  (CLAUDE.md, skills, plugins, hooks, MCP
  ```

- `top_help_line_128` (`top_help`, display-untranslated-line)

  ```text
  æå¡å¨, custom commands and agents,
  ```

- `top_help_line_129` (`top_help`, display-untranslated-line)

  ```text
  output styles, workflows, custom themes,
  ```

- `top_help_line_130` (`top_help`, display-untranslated-line)

  ```text
  keybindings, and more) disabled — useful
  ```

- `top_help_line_131` (`top_help`, display-untranslated-line)

  ```text
  for troubleshooting a broken
  ```

- `top_help_line_132` (`top_help`, display-untranslated-line)

  ```text
  configuration. Admin-managed (policy)
  ```

- `top_help_line_133` (`top_help`, display-untranslated-line)

  ```text
  settings still apply. Auth, model
  ```

- `top_help_line_134` (`top_help`, display-untranslated-line)

  ```text
  selection, built-in tools, and
  ```

- `top_help_line_135` (`top_help`, display-untranslated-line)

  ```text
  permissions work normally. Sets
  ```

- `top_help_line_138` (`top_help`, display-untranslated-line)

  ```text
  --setting-sources <sources>           è¦å è½½çè®¾ç½®æ¥æºåè¡¨ï¼éå·åéï¼userãprojectãlocalï¼ã
  ```

- `top_help_line_152` (`top_help`, display-untranslated-line)

  ```text
  -w, --worktree [name]                 ä¸ºæ­¤ä¼è¯åå»ºæ°ç git worktreeï¼å¯æå®åç§°ï¼
  ```

- `top_help_line_157` (`top_help`, display-untranslated-line)

  ```text
  auto-mode                             Inspect or reset auto mode classifier
  ```

- `top_help_line_159` (`top_help`, display-untranslated-line)

  ```text
  doctor                                Check the health of your Claude Code
  ```

- `top_help_line_160` (`top_help`, display-untranslated-line)

  ```text
  installation. Reads settings files in
  ```

- `top_help_line_161` (`top_help`, display-untranslated-line)

  ```text
  the current directory without a trust
  ```

- `top_help_line_162` (`top_help`, display-untranslated-line)

  ```text
  prompt. For a full checkup that can also
  ```

- `top_help_line_163` (`top_help`, display-untranslated-line)

  ```text
  fix issues, run /doctor in a session.
  ```

- `top_help_line_164` (`top_help`, display-untranslated-line)

  ```text
  gateway [options]                     Run the enterprise auth/telemetry
  ```

- `top_help_line_166` (`top_help`, display-untranslated-line)

  ```text
  import [options] [source]             Import config from another AI coding
  ```

- `agents_help_line_7` (`agents_help`, display-untranslated-line)

  ```text
  --agent <agent>                       Default agent for sessions dispatched
  ```

- `agents_help_line_8` (`agents_help`, display-untranslated-line)

  ```text
  from agent view. Overrides the 'agent'
  ```

- `agents_help_line_10` (`agents_help`, display-untranslated-line)

  ```text
  --all                                 With --json: also include completed
  ```

- `agents_help_line_11` (`agents_help`, display-untranslated-line)

  ```text
  background sessions
  ```

- `agents_help_line_18` (`agents_help`, display-untranslated-line)

  ```text
  --json                                Print active sessions (interactive and
  ```

- `agents_help_line_19` (`agents_help`, display-untranslated-line)

  ```text
  background) as a JSON array and exit
  ```

- `agents_help_line_20` (`agents_help`, display-untranslated-line)

  ```text
  (for scripting; does not require a TTY)
  ```

- `agents_help_line_25` (`agents_help`, display-untranslated-line)

  ```text
  --setting-sources <sources>           è¦å è½½çè®¾ç½®æ¥æºåè¡¨ï¼éå·åéï¼userãprojectãlocalï¼ã
  ```

- `auto_mode_help_line_3` (`auto_mode_help`, display-untranslated-line)

  ```text
  Inspect or reset auto mode classifier configuration
  ```

- `auto_mode_help_line_9` (`auto_mode_help`, display-untranslated-line)

  ```text
  config              ä»¥ JSON æå°å®éçæç auto mode éç½®ï¼å·²è®¾ç½®é¡¹ä½¿ç¨ä½
  ```

- `auto_mode_help_line_11` (`auto_mode_help`, display-untranslated-line)

  ```text
  critique [options]  è·å AI å¯¹ä½ èªå®ä¹ auto mode è§åçåé¦
  ```

- `auto_mode_help_line_12` (`auto_mode_help`, display-untranslated-line)

  ```text
  defaults [options]  ä»¥ JSON æå°é»è®¤ auto mode ç¯å¢ãallowãsoft_deny å hard_deny
  ```

- `auto_mode_help_line_15` (`auto_mode_help`, display-untranslated-line)

  ```text
  reset [options]     Reset auto mode configuration to the shipped defaults by
  ```

- `auto_mode_help_line_16` (`auto_mode_help`, display-untranslated-line)

  ```text
  removing the autoMode section from your user settings file
  ```

- `doctor_help_line_3` (`doctor_help`, display-untranslated-line)

  ```text
  Check the health of your Claude Code installation. Reads settings files in the
  ```

- `doctor_help_line_4` (`doctor_help`, display-untranslated-line)

  ```text
  current directory without a trust prompt. For a full checkup that can also fix
  ```

- `doctor_help_line_5` (`doctor_help`, display-untranslated-line)

  ```text
  issues, run /doctor in a session.
  ```

- `mcp_help_line_21` (`mcp_help`, display-untranslated-line)

  ```text
  # æ·»å å¸¦å­è¿ç¨ flag ç stdio æå¡å¨ï¼
  ```

- `mcp_help_line_32` (`mcp_help`, display-untranslated-line)

  ```text
  login [options] <name>                Authenticate with an MCP server (HTTP,
  ```

- `mcp_help_line_34` (`mcp_help`, display-untranslated-line)

  ```text
  logout <name>                         Clear stored OAuth credentials for an
  ```

- `plugin_help_line_1` (`plugin_help`, display-untranslated-line)

  ```text
  ç¨æ³ï¼claude plugin|plugins [options] [command]
  ```

- `plugin_help_line_12` (`plugin_help`, display-untranslated-line)

  ```text
  eval [options] [target]              Run eval cases (evals/**/case.yaml or
  ```

- `plugin_help_line_13` (`plugin_help`, display-untranslated-line)

  ```text
  evals/**/prompt.md + graders/*.md)
  ```

- `plugin_help_line_14` (`plugin_help`, display-untranslated-line)

  ```text
  against a plugin and report scored
  ```

- `plugin_help_line_15` (`plugin_help`, display-untranslated-line)

  ```text
  results. Target is a path, a plugin name,
  ```

- `plugin_help_line_17` (`plugin_help`, display-untranslated-line)

  ```text
  and skills-dir plugins both resolve (and
  ```

- `plugin_help_line_18` (`plugin_help`, display-untranslated-line)

  ```text
  add a no-plugin baseline arm)
  ```

- `plugin_help_line_20` (`plugin_help`, display-untranslated-line)

  ```text
  init|new [options] <name>            Scaffold a new plugin at
  ```

- `plugin_help_line_21` (`plugin_help`, display-untranslated-line)

  ```text
  ~/.claude/skills/<name>/ (auto-loads next
  ```

- `plugin_help_line_22` (`plugin_help`, display-untranslated-line)

  ```text
  session as <name>@skills-dir)
  ```

- `plugin_help_line_28` (`plugin_help`, display-untranslated-line)

  ```text
  tag [options] [path]                 ä¸ºæä»¶åå¸åå»º {name}--v{version} git
  ```

- `plugin_help_line_29` (`plugin_help`, display-untranslated-line)

  ```text
  tagï¼å¹¶æ ¡éª plugin.json ä¸æå±æä»¶å¸åºæ¡ç®ä¸è´
  ```

- `update_help_line_1` (`update_help`, display-untranslated-line)

  ```text
  ç¨æ³ï¼claude update|upgrade [options]
  ```

- `ultrareview_help_line_7` (`ultrareview_help`, display-untranslated-line)

  ```text
  --json               æå°åå§ bugs.json payloadï¼èä¸æ¯æ ¼å¼ååç findings
  ```

- `ultrareview_help_line_8` (`ultrareview_help`, display-untranslated-line)

  ```text
  --no-post            Do not post the findings to the PR (the default; accepted
  ```

- `ultrareview_help_line_9` (`ultrareview_help`, display-untranslated-line)

  ```text
  for parity with the /ultrareview and /code-review ultra
  ```

- `ultrareview_help_line_11` (`ultrareview_help`, display-untranslated-line)

  ```text
  --post               Post the finished review's findings to the PR as you (PR
  ```

- `ultrareview_help_line_12` (`ultrareview_help`, display-untranslated-line)

  ```text
  targets only; one plain comment, not a review)
  ```

## Text Diff Excerpt

```markdown
# Upstream text diff: 2.1.156 -> 2.1.227

- Added upstream strings: 34242
- Removed upstream strings: 16780
- Already covered by translations: 100
- Needs translation review: 34142
- Sensitive review hints: 714

## Added strings needing review
- ___ESCAPED_AMPERSAND_${...}___
- __artifactConsentAskCanReachUser
- __artifactPlanConsentAsk
- __artifactPlanConsentDecisionCaps
- __artifactPublishTarget
- __autonomous_preamble__
- __cc_set=\${$__cc_name+x}
- __CFBundleIdentifier
- __explicit_off__
- __Host-gw_dev
- __not_strictly_false__
- __org_memory_project_off__
- __org_memory_project_picker__
- __readyz_probe__
- __remote-workflow
- __schema${...}
- __unparsedToolInput
- _${...}_SBX
- _arr
- _chunkTimes
- _dispose
- _errs
- _globalThis
- _idmap
- _in_
- _meta
- _nothing found_
- _Pragma
- _prefault
- _This guidance is pinned to runtime contract ${...} \u2014 the contract the target${...} currently runs. A carry-forward republish keeps this pin._
- _thumb.html
- _thumb.img
- - [Title](file.md) \u2014 one-line hook`. The index has no frontmatter. Never write memory content directly into the index.
- - **Before answering questions about anything in the doc list above**, read or search the relevant doc with `project_read` or `project_search`. Do not Glob/Grep the local filesystem for these \u2014 they live in the project, not on disk.
- - **CONFIRMED** \u2014 can name the inputs/state that trigger it and the wrong output or crash. Quote the line. - **PLAUSIBLE** \u2014 mechanism is real, trigger is uncertain (timing, env, config). State what would confirm it. - **REFUTED** \u2014 factually wrong (code doesn't say that) or guarded elsewhere. Quote the line that proves it.
- - **To edit a project doc**, `project_read` it, make the change, and `project_write` the full updated content back to the same path. There is no in-place patch.
- - **When you produce something durable and relevant to this project** \u2014 a new doc, an update to an existing one, a captured decision or finding the user or their team would look for here later \u2014 write it to the project with `project_write`. The project is what they see across Claude products. Be selective: write things that belong alongside the existing docs, not every artifact or note.
- - **You don't have to use the project for everything.** If the request is unrelated to it, answer normally without reading or writing the project.
- - \u2026 and ${...} more \u2014 call `project_info` for the full list
- - `${...}` \u2014 for files under ${...}/
- ... 34102 more

## Added strings already covered
- Add a new working directory
- Add an MCP server (stdio or SSE) with a JSON string
- Additional CA cert(s)
- Amazon Bedrock (Mantle) base URL
- Anthropic base URL
- Arguments:
- Ask a quick side question without interrupting the main conversation
- Auth token
- Bedrock base URL
- Commands:
- Compacting conversation
- Completing auth flow with authorization code
- Configure and manage MCP servers
- Configure optional break reminders and quiet-hours nudges
- Connection error.
- Conversation copied to clipboard
- Copy the conversation to your system clipboard
- Copy to clipboard
- Currently using:
- Default - trusted network access
- Default region
- Detach from this background session (it keeps running)
- display help for command
- Edit Failed
- Enter filename:
- Esc again to clear
- Exit the CLI
- Export the current conversation to a file or clipboard
- File Changed
- Fixed port for OAuth callback (for servers requiring pre-registered redirect URIs)
- Get details about an MCP server. Unapproved .mcp.json servers are shown as \u23F8 Pending approval and not connected to; approved servers are health-checked.
- Have an administrator set channelsEnabled: true in managed settings to enable
- Image was too large. Double press esc to go back and try again with a smaller image.
- Import MCP Servers from Claude Desktop
- Import MCP servers from Claude Desktop (Mac and WSL only)
- Inbound messages will be silently dropped
- Keep worktree
- Keep worktree and tmux session
- List configured MCP servers. Unapproved .mcp.json servers are shown as \u23F8 Pending approval and not connected to; approved servers are health-checked.
- Listen to Claude FM lo-fi radio
- ... 60 more

## Removed strings
- __halt_compiler
- __halt_compiler.+?;
- __isLong__
- __leader__
- __manual__
- __other__
- __shared
- _Bool _Complex _Imaginary
- _bucket
- _claude_fs_right:
- _hidden
- _max
- _min
- _sum
- - `${...}` is always loaded into your conversation context \u2014 lines after ${...} will be truncated, so keep the index concise
- - ${...} (task ${...})
- - ${...} Suggested fix: ${...}
- - Anything already documented in CLAUDE.md files.
- - Code patterns, conventions, architecture, file paths, or project structure \u2014 these can be derived by reading the current project state.
- - Debugging solutions or fix recipes \u2014 the fix is in the code; the commit message has the context.
- - Delete existing memories that are superceded by the memory you have saved.
- - Do not write duplicate memories. First check if there is an existing memory that already covers what you want to save.
- - End git commit messages with: ${...}
- - End PR bodies with: ${...}
- - Environment variable ${...}: ${...}
- - Ephemeral task details: in-progress work, temporary state, current conversation context.
- - Git history, recent changes, or who-changed-what \u2014 `git log` / `git blame` are authoritative.
- - If the memory names a file path: check the file exists.
- - If the user is about to act on your recommendation (not just asking about history), verify first.
- - Keybinding (${...}): ${...}${...}
- - Memory records can become stale over time. Use memory as context for what was true at a given point in time. Before answering the user or building assumptions based solely on information in memory records, verify that the memory is still correct and up-to-date by reading the current state of the files or resources. If a recalled memory conflicts with current information, trust what you observe now \u2014 and update or remove the stale memory rather than acting on it.
- - Network latency: ${...}ms (${...}%)
- - Plugin note${...}: ${...}
- - Plugin setting: ${...}
- - Plugin${...}: ${...}
- - Pre-request overhead: ${...}ms (${...}%)
- - Sandbox: ${...} (See /sandbox for install instructions)
- - Settings${...}: ${...}${...}
- - Stops a running background task by its ID - Takes a task_id parameter identifying the task to stop - Returns a success or failure status - Use this tool when you need to terminate a long-running task
- - Type '${...}' does not match '${...}'
- ... 16740 more
```

## Takeover Commands

```bash
node scripts/verify-upstream-compat.js --baseline 2.1.227 --skip-latest --native-macos-arm64 --json
node scripts/generate-upstream-text-diff.js --to 2.1.227 --native-macos-arm64
node scripts/promote-native-candidate.js --candidate <candidate-json>
```

## Done Criteria

- Add the missing source-of-truth translations or guard fix on this branch.
- Re-run the native latest candidate workflow for this version.
- Only promote support metadata after native verification and display audit pass.
- Remove or update this handoff report before marking the PR ready.
