# Native latest candidate failure: 2.1.158

- Run: https://github.com/wang-kaopu/claude-code-zh-cn/actions/runs/26676954762
- Head SHA: `5889dc056e205722d637303b61e6140e50b9be51`
- Status: `fail`
- Candidate kind: `native`
- Native: `darwin-arm64` / `ok` / `ok` / `ok`
- Display audit: `fail` (28 issues / 11 commands)

## What Failed

- Candidate verification did not pass: `status=fail`.
- Display audit did not pass: `status=fail`.
- Summary: pass=0, fail=1, skip=0.

## Display Audit Issues

- `top_json_schema_term` (`top_help`, display-preserve)

  ```text
  JSON Schema
  ```

- `top_help_line_13` (`top_help`, display-untranslated-line)

  ```text
  code", "prompt": "You are a code
  ```

- `top_help_line_45` (`top_help`, display-untranslated-line)

  ```text
  high, xhigh, max）
  ```

- `top_help_line_51` (`top_help`, display-untranslated-line)

  ```text
  --fallback-model <model>              Enable automatic fallback to specified
  ```

- `top_help_line_52` (`top_help`, display-untranslated-line)

  ```text
  model when the default model is
  ```

- `top_help_line_53` (`top_help`, display-untranslated-line)

  ```text
  overloaded or not available (only works
  ```

- `top_help_line_54` (`top_help`, display-untranslated-line)

  ```text
  with --print)
  ```

- `top_help_line_56` (`top_help`, display-untranslated-line)

  ```text
  --file file_abc:doc.txt
  ```

- `top_help_line_57` (`top_help`, display-untranslated-line)

  ```text
  file_def:img.png）
  ```

- `top_help_line_82` (`top_help`, display-untranslated-line)

  ```text
  --model <model>                       Model for the current session. Provide
  ```

- `top_help_line_83` (`top_help`, display-untranslated-line)

  ```text
  an alias for the latest model (e.g.
  ```

- `top_help_line_84` (`top_help`, display-untranslated-line)

  ```text
  'sonnet' or 'opus') or a model's full
  ```

- `top_help_line_112` (`top_help`, display-untranslated-line)

  ```text
  --prompt-suggestions [value]          Enable prompt suggestions. In print/SDK
  ```

- `top_help_line_113` (`top_help`, display-untranslated-line)

  ```text
  mode, emits a prompt_suggestion message
  ```

- `top_help_line_114` (`top_help`, display-untranslated-line)

  ```text
  after each turn with a predicted next
  ```

- `agents_help_line_7` (`agents_help`, display-untranslated-line)

  ```text
  --agent <agent>                       Default agent for sessions dispatched
  ```

- `agents_help_line_8` (`agents_help`, display-untranslated-line)

  ```text
  from agent view. Overrides the 'agent'
  ```

- `mcp_help_line_27` (`mcp_help`, display-untranslated-line)

  ```text
  get <name>                            Get details about an MCP server.
  ```

- `mcp_help_line_28` (`mcp_help`, display-untranslated-line)

  ```text
  Unapproved .mcp.json servers are shown
  ```

- `mcp_help_line_29` (`mcp_help`, display-untranslated-line)

  ```text
  as ⏸ Pending approval and not connected
  ```

- `mcp_help_line_30` (`mcp_help`, display-untranslated-line)

  ```text
  to; approved servers are health-checked.
  ```

- `mcp_help_line_32` (`mcp_help`, display-untranslated-line)

  ```text
  list                                  List configured MCP 服务器. Unapproved
  ```

- `mcp_help_line_33` (`mcp_help`, display-untranslated-line)

  ```text
  .mcp.json servers are shown as ⏸ Pending
  ```

- `mcp_help_line_34` (`mcp_help`, display-untranslated-line)

  ```text
  approval and not connected to; approved
  ```

- `mcp_help_line_35` (`mcp_help`, display-untranslated-line)

  ```text
  servers are health-checked.
  ```

- `plugin_help_line_13` (`plugin_help`, display-untranslated-line)

  ```text
  init|new [options] <name>            Scaffold a new plugin at
  ```

- `plugin_help_line_14` (`plugin_help`, display-untranslated-line)

  ```text
  ~/.claude/skills/<name>/ (auto-loads next
  ```

- `plugin_help_line_15` (`plugin_help`, display-untranslated-line)

  ```text
  session as <name>@skills-dir)
  ```

## Text Diff Excerpt

```markdown
# Upstream text diff: 2.1.150 -> 2.1.158

- Added upstream strings: 11183
- Removed upstream strings: 6910
- Already covered by translations: 222
- Needs translation review: 10961
- Sensitive review hints: 304

## Added strings needing review
- __autonomous_preamble__
- __manual__
- __other__
- _${...}_SBX
- _claude_fs_right:
- _local_
- _staging_
- _zod
- - **${...}** (if available) - Run a multi-step subagent pipeline; prefer it over hand-orchestrating ${...} calls when a matching workflow exists
- - **id**: Task identifier (use with TaskGet, TaskUpdate)
- - `run_in_background` runs the command detached: it keeps running across turns and re-invokes you when it exits. No `&` needed.
- - Before assigning tasks to teammates, to see what's available
- - Before producing ANY summary or analysis, you MUST explicitly describe what portion of the content you have read. ***If you did not read the entire content, you MUST explicitly state this.*** - If after a few attempts you cannot read the file (file not found, lines too long for Read's offset/limit, no shell access), STOP retrying. Summarize what you were able to read, explicitly state which portion you could not read and why, and proceed.
- - Enabled "Use Option as Meta key"
- - End git commit messages with: ${...}
- - End PR bodies with: ${...}
- - For analysis or summarization that requires reading the full content: ${...} - If the ${...} tool is available, do this inside a subagent so the full output stays out of your main context. Give it the instruction above verbatim, and be explicit about what it must return \u2014 e.g. "${...}" A vague "summarize this" may lose detail.
- - For targeted queries (find a value, filter by field): use jq on the file directly.
- - For targeted searches (find a line, locate a string): use grep on the file directly.
- - For targeted searches (find a string): use grep on the file directly.
- - If you receive truncation warnings when reading the file ("[N lines truncated]"), reduce the chunk size until you have read 100% of the content without truncation ***DO NOT PROCEED UNTIL YOU HAVE DONE THIS***. Bash output is limited to ${...} chars.
- - If you receive truncation warnings when reading the file, reduce the chunk size until you have read 100% of the content without truncation.
- - Include enough detail in the description for another agent to understand and complete the task - New tasks are created with status 'pending' and no owner - use TaskUpdate with the `owner` parameter to assign them
- - Line ${...}
- - Note: this file's lines are too long for Read's offset/limit chunking. If a shell tool is available, slice by character range (e.g. python read()[A:B], dd, or cut -c) instead.
- - Switched to visual bell
- - Working directory persists between calls, but prefer absolute paths \u2014 `cd` in a compound command can trigger a permission prompt. Shell state (env vars, functions) does not persist; the shell is initialized from the user's profile.
- - You MUST read the content from the file at ${...} in sequential chunks until 100% of the content has been read.
- --- # Current Build Generated from the running Claude Code binary at invocation time. This is ground truth \u2014 it overrides your training data and any documentation when they disagree about what exists in this build. ${...}
- --- ## User Request ${...}
- --- NOTE: You are running inside a workflow script. Your final text response is returned verbatim as a string to the calling script \u2014 it is your return value, not a message to a human. Output the literal result; do not output confirmations like "Done." Be concise \u2014 the script will parse your output.
- --add-dir
- --agent
- --align
- --allowedTools
- --attach
- --auth-type
- --author <name>
- --author-email <email>
- --author=${...}
- ... 10921 more

## Added strings already covered
- ((empty message))
- (already granted)
- (No changes)
- (No content)
- (not installed)
- [awaiting approval]
- [Image data detected and sent to Claude]
- [Image]
- [stopping]
- [View Tab]
- Access denied to specific paths outside allowed directories
- Accessibility and Screen Recording permissions are required. Call request_access to show the permission panel.
- Add directory to workspace
- Agent idle
- Also requested:
- Amazon Bedrock, Microsoft Foundry, or Vertex AI
- and tell Claude what to do differently
- and tell Claude what to do next
- Another Claude session is currently using the computer. Wait for that session to finish, or find a non-computer-use approach.
- API usage billing
- Auto-registered claude-cli:// deep link protocol handler
- Balanced performance - best for most agents
- Changing thinking mode mid-conversation will increase latency and may reduce quality. For best results, set this at the start of a session.
- Chat about this
- Checking for updates
- Choose an option:
- Claude Code will be able to read files in this directory and make edits when auto-accept edits is on.
- Claude is now exploring and designing an implementation approach.
- Claude is waiting for your input
- Claude wants to enter plan mode to explore and design an implementation approach.
- Claude wants to exit plan mode
- Claude will respond without extended thinking
- Claude will think before responding
- Commands cannot run outside the sandbox under any circumstances.
- Compact buffer
- Computer control is disabled in Settings. Enable it and try again.
- Computer Use needs macOS permissions
- Computer Use wants to control these apps
- Configuration Error
- Confirm you want to restore
- ... 182 more

## Removed strings
- _A-Za-z\xC0-\xD6\xD8-\xF6\xF8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD
- _attributeChangeHandlers
- - Affects multiple files, user should approve the approach ### BAD - Don't use EnterPlanMode: User:
- - Architectural decision on theme system, affects many components User:
- - could use Redis, in-memory, file-based, etc. - Example:
- - Do not use the exit plan mode tool because you are not planning the implementation steps of a task. 2. Initial task:
- - e.g.
- - Example:
- - many optimization strategies possible 3. **Code Modifications**: Changes that affect existing behavior or structure - Example:
- - Multiple approaches possible, need to profile first, significant impact User:
- - need to profile and identify bottlenecks - Example:
- - prompt:
- - Redux vs Context vs custom solution 5. **Multi-File Changes**: The task will likely touch more than 2-3 files - Example:
- - Requires architectural decisions (session vs JWT, where to store tokens, middleware structure) User:
- - Seems simple but involves: where to place it, confirmation dialog, API call, error handling, state updates User:
- - Simple, obvious implementation User:
- - Straightforward, no planning needed User:
- - that's exactly what THIS tool does. ExitPlanMode inherently requests user approval of your plan. ## Examples 1. Initial task:
- - Use the exit plan mode tool after you have finished planning the implementation steps of the task. 3. Initial task:
- - WebSockets vs SSE vs polling - Example:
- - what exactly should change? - Example:
- - what rules? What error messages? 2. **Multiple Valid Approaches**: The task can be solved in several different ways - Example:
- - what's the target architecture? 4. **Architectural Decisions**: The task requires choosing between patterns or technologies - Example:
- - where should it go? What should happen on click? - Example:
- -- Cleaning MCPB...
- -- Manifest already valid per MCPB schema
- -- No node_modules, not pruning
- -- node_modules found, deleting development dependencies
- -- Removed development dependencies from node_modules
- -- Some modules already removed, skipping remaining cleanup
- -- Unpacking MCPB...
- -- Update manifest to be valid per MCPB schema
- ----FormBoundary${...}
- --abbrev
- --abbrev-commit
- --absolute-git-dir
- --active
- --after
- --agent-type ${...}
- --ahead-behind
- ... 6870 more
```

## Takeover Commands

```bash
node scripts/verify-upstream-compat.js --baseline 2.1.158 --skip-latest --native-macos-arm64 --json
node scripts/generate-upstream-text-diff.js --to 2.1.158 --native-macos-arm64
node scripts/promote-native-candidate.js --candidate <candidate-json>
```

## Done Criteria

- Add the missing source-of-truth translations or guard fix on this branch.
- Re-run the native latest candidate workflow for this version.
- Only promote support metadata after native verification and display audit pass.
- Remove or update this handoff report before marking the PR ready.
